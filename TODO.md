# Bugs to be fixed
- when splitting a connected clip with B, when there is no main timeline clip left to connect one of the resulting connected clips, then the empty main clip should be created and the resulting connected clip should connect there
- with a single click the user should be able to select only one clip, regardless if it is a main clip or connected clip

# New Features
1. With Ctrl Click, the user should be able to select multiple clips
2. When multiple clips selected, the backspace key should delete them all
3. There should be a "Properties" pane to the right. For now just display the slider for the audio volume. The panel should affect only the selected clip(s)
4. There should be an ability to speed up or slow down a selected clip: 0.25x, 0.5x, 1x, 1.5x, 2x, 3x, 8x. This should be in the Properties panel